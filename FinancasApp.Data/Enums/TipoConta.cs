namespace FinancasApp.Data.Enums
{

    // Opcoes que NUNCA irão mudar, se nao muda o escopo do projeto! Nao existe um tipoconta que nao seja Receita ou Despesa!
    public enum TipoConta
    {
        Receita = 1,
        Despesa = 2
    }
}
